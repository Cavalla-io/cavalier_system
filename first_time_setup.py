#TODO: write a script to set up the system for the first time

"""
Need to be able to git pull the controls and sensors repos, will need some ssh key to make this work. We could host a server that has an endpoint that just serves ssh keys for this purpose.

Also need this to build the ros packages and verify everything


"""
